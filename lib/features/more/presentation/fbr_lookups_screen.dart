import 'package:flutter/material.dart';

class FbrLookupsScreen extends StatelessWidget {
  const FbrLookupsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('FBR Lookups')));
  }
}
